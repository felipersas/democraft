/**
 * Composition dimensions shared across the studio. Centralized so the
 * "1920×1080" assumption lives in one place — when portrait or alternate
 * resolutions are added, this is the single switch.
 */
export const STUDIO_WIDTH = 1920;
export const STUDIO_HEIGHT = 1080;
