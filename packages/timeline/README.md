# @democraft/timeline

Resolves compiled IR and Playwright capture manifests into deterministic render timelines.

This package does not render React or Remotion compositions. It only produces the frame-based data a renderer can consume.
