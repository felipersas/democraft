# Expected — 03-form-flow

This directory holds the known-good reference for the scenario. It is never
shown to an agent under test. A reviewed `discovery.snapshot.json` and `demo.ts`
can be added after the harness output is accepted as a baseline.
